spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
app:
  proxy:
    api-key: test
    url: https://proxy6.net/api/

server:
  port: 8022

application:
  redis:
    cache-cron: 0 0 6 * * ?
